/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["cuqvbjobsgfbfahjrzeq.supabase.co"], // Adicione seu domínio Supabase aqui
    },
  };

export default nextConfig;
